{
  "name": "prismcore-docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev . --host 0.0.0.0 --port 5174 --strictPort",
    "build": "vitepress build .",
    "preview": "vitepress preview . --host 0.0.0.0 --port 4174 --strictPort",
    "build:laravel": "vitepress build . && node scripts/copy-to-laravel.mjs"
  },
  "devDependencies": {
    "vitepress": "^1.6.0"
  }
}
